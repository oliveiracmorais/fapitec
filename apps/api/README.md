# apps/api

Backend principal da plataforma modular da `FAPITEC-SE`.

Stack alvo:

- `Go`
- `PostgreSQL`
- `sqlc`
- `DDD + Clean Architecture`

Os contextos de dominio devem evoluir em portugues e separar, quando fizer sentido, `dominio`, `aplicacao`, `interfaces` e `infraestrutura`.
