# Story 2.2.3: Submissão de Propostas — Frontend

## Status

Ready for Review

## Story

**Como** proponente da FAPITEC-SE,
**quero** submeter propostas de pesquisa com um formulário multi-etapas,
**para que** eu possa concorrer aos recursos de fomento dos editais vigentes.

## Context

O backend de submissão de propostas (Story 2.2.2) já está concluído com endpoints REST, entidades de domínio e validações. O frontend atual possui páginas de editais (listagem, criação, detalhe) como referência de padrão. O domínio `inscricao_e_selecao_de_projetos` no backend expõe: `POST /api/v1/propostas`, `GET /api/v1/propostas`, `PUT /api/v1/propostas/{id}`, `DELETE /api/v1/propostas/{id}`, `POST /api/v1/propostas/{id}/submeter`.

O ProjetoFapitec.txt (Seção 5.8.2) especifica: interface responsiva, upload de documentos, edição dentro do prazo, geração de protocolo, histórico de versões, cálculo automático de orçamento.

## Acceptance Criteria

1. O proponente deve acessar um formulário multi-etapas ao clicar "Inscrever-se" em um edital ativo.
2. O formulário deve conter ao menos 4 etapas sequenciais: Dados do Proponente, Formação Acadêmica, Documentação, Planilha Orçamentária.
3. Dados do Proponente: nome, CPF, RG, gênero, etnia, data de nascimento, endereço, telefone — pré-preenchidos da sessão.
4. Formação Acadêmica: maior titulação, curso, instituição, ano de conclusão, área de conhecimento.
5. Documentação: upload de arquivos com tipos exigidos pelo edital (PDF, DOC, imagens), com validação de formato e tamanho.
6. Planilha Orçamentária: adicionar itens (descrição, tipo consumo/permanente, quantidade, valor unitário), cálculo automático do total.
7. O sistema deve exibir o valor total calculado e validar se excede o valor global do edital.
8. Ao submeter, o sistema deve exibir o protocolo gerado e redirecionar para o painel de acompanhamento.
9. O proponente deve poder editar a proposta enquanto o edital estiver vigente (botão "Editar" no painel).
10. O painel do proponente deve listar todas as suas propostas com status, protocolo, edital e ações disponíveis.
11. Os valores monetários devem usar formatação BRL (R$ 1.234,56).
12. A implementação deve seguir o mesmo padrão de componentes das páginas de editais existentes.

## Tasks / Subtasks

### Componentes de Domínio (extraídos para `components/`)
- [x] Criar `components/proposta-painel.tsx` — painel do proponente com lista de propostas
- [x] Criar `components/proposta-form-multi-etapas.tsx` — formulário multi-etapas (wrapper)
- [x] Criar `components/proposta-etapa-dados-pessoais.tsx` — etapa 1
- [x] Criar `components/proposta-etapa-formacao-academica.tsx` — etapa 2
- [x] Criar `components/proposta-etapa-documentos.tsx` — etapa 3 (upload)
- [x] Criar `components/proposta-etapa-orcamento.tsx` — etapa 4 (planilha)
- [x] Criar `components/proposta-resumo.tsx` — resumo antes da submissão
- [x] Criar `components/proposta-item-orcamento.tsx` — linha de item orçamentário
- [x] Criar `components/proposta-upload.tsx` — componente de upload com validação
- [x] Criar `components/proposta-status-badge.tsx` — badge de status da proposta

### Páginas
- [x] Criar `app/(app)/minhas-propostas/page.tsx` — painel do proponente (lista)
- [x] Criar `app/(app)/minhas-propostas/[id]/page.tsx` — detalhe da proposta
- [x] Criar `app/(app)/editais/[id]/inscrever/page.tsx` — formulário de submissão
- [x] Atualizar `app/(app)/editais/[id]/page.tsx` — adicionar botão "Inscrever-se" se edital ativo

### Navegação
- [x] Adicionar link "Minhas Propostas" no Header
- [x] Adicionar rota `/minhas-propostas` no middleware de proteção

### Integração com API
- [x] Criar serviço `lib/api-propostas.ts` — funções fetch para endpoints de proposta
- [x] Integrar com `POST /api/v1/propostas` (criar rascunho)
- [x] Integrar com `POST /api/v1/propostas/{id}/submeter` (submeter)
- [x] Integrar com `GET /api/v1/propostas` (listar do proponente)
- [x] Integrar com `GET /api/v1/propostas/{id}` (detalhe)
- [x] Integrar com `PUT /api/v1/propostas/{id}` (editar)
- [x] Integrar com `DELETE /api/v1/propostas/{id}` (deletar rascunho)
- [x] Integrar com `GET /api/v1/editais/{id}/propostas` (opcional)
- [x] Integrar com `GET /api/v1/editais` (para verificar editais ativos)

### Validação
- [x] Adicionar validações de formulário em `lib/validacao.ts`:
  - [x] `validarTitulacao` — maior titulação obrigatória
  - [x] `validarAnoConclusao` — ano válido
  - [x] `validarValorUnitario` — valor positivo
  - [x] `validarQuantidade` — inteiro positivo
  - [x] `validarArquivo` — formato e tamanho (5MB limite)

### Estados e Tratamento
- [x] Loading state em cada etapa do formulário
- [x] Empty state no painel ("Nenhuma proposta encontrada")
- [x] Error state para falhas de API (com retry)
- [x] Sucesso state com exibição do protocolo

### Qualidade
- [x] `pnpm run lint` ✅
- [x] `pnpm run typecheck` ✅
- [x] Verificar acessibilidade (aria-labels, roles) — presente nos componentes

## Dev Notes

- **Padrão existente:** seguir o mesmo padrão das páginas de editais: `"use client"`, `useState` + `useEffect`, `fetch()` direto, Tailwind CSS, `CampoFormulario` para inputs
- **Upload:** usar `<input type="file">` nativo com validação client-side; backend espera FormData ou base64 (definir em conjunto com o adapter de upload)
- **Orçamento:** usar `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })` para exibição; armazenar em centavos (int) no formulário
- **Etapas:** controlar etapa atual com `useState<number>`; validar etapa antes de avançar; permitir voltar
- **Rascunho:** criar proposta como rascunho ao iniciar o formulário (POST) para evitar perda de dados
- **Documentos:** nesta story, o upload será via endpoint separado a ser definido ou embutido no JSON como base64 (simplificação inicial)
- **Pré-preenchimento:** dados do proponente vêm da sessão (`useAuth().usuario`)
- **Middleware:** path `/minhas-propostas` já deve ser protegido pelo middleware existente (padrão `/(app)/*`)
- **Sem shadcn/ui:** o projeto não utiliza shadcn/ui; manter Tailwind puro + componentes inline

## Testing

- Testar navegação entre etapas (próximo/anterior)
- Testar validação por etapa (campos obrigatórios)
- Testar fluxo completo: criar rascunho → preencher → submeter → ver protocolo
- Testar edição de proposta existente
- Testar exclusão de rascunho
- Testar upload de arquivo (formato inválido, tamanho excessivo)
- Testar cálculo de orçamento (itens × valor unitário)
- Testar responsivo (mobile, tablet, desktop)

## Dependencies

- Story 2.2.2: Submissão de Propostas — Backend (endpoints REST funcionando)
- Story 2.2.1: Extensão do Modelo de Editais (campos de elegibilidade e documentos obrigatórios)
- Epic 2.2: Gestão de Editais e Submissão de Propostas

## File List

### Criados
- `apps/web/src/lib/api-propostas.ts`
- `apps/web/src/components/proposta-status-badge.tsx`
- `apps/web/src/components/proposta-item-orcamento.tsx`
- `apps/web/src/components/proposta-upload.tsx`
- `apps/web/src/components/proposta-etapa-dados-pessoais.tsx`
- `apps/web/src/components/proposta-etapa-formacao-academica.tsx`
- `apps/web/src/components/proposta-etapa-documentos.tsx`
- `apps/web/src/components/proposta-etapa-orcamento.tsx`
- `apps/web/src/components/proposta-resumo.tsx`
- `apps/web/src/components/proposta-form-multi-etapas.tsx`
- `apps/web/src/components/proposta-painel.tsx`
- `apps/web/src/app/(app)/minhas-propostas/page.tsx`
- `apps/web/src/app/(app)/minhas-propostas/[id]/page.tsx`
- `apps/web/src/app/(app)/editais/[id]/inscrever/page.tsx`

### Modificados
- `apps/web/src/lib/validacao.ts` (add validarTitulacao, validarAnoConclusao, validarValorUnitario, validarQuantidade, validarArquivo)
- `apps/web/src/components/header.tsx` (add navegação: Dashboard, Editais, Minhas Propostas)
- `apps/web/src/components/breadcrumb.tsx` (add rotas minhas-propostas, inscrever)
- `apps/web/src/app/(app)/editais/[id]/page.tsx` (add botão "Inscrever-se neste edital")

## Change Log

| Data | Versão | Descrição | Autor |
| --- | --- | --- | --- |
| 2026-06-06 | 1.0 | Draft inicial da Story 2.2.3 — Frontend de Submissão de Propostas | @sm (River) |
| 2026-06-06 | 1.1 | Implementação completa: 14 arquivos criados, 4 modificados, lint + typecheck + testes passando | @dev (Dex) |
